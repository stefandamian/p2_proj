{% extends 'base.html' %}

{% block content %}

{% load index %}
<div class="container-fluid">
	<div class="row">
		<div class="col-md-12">
			<div class="page-header margin-25">
				<h1>Produse</h1>
			</div>
			<table class="table table-bordered table-sm">
				<thead>
					<tr>
						<th>#</th>
						<th>Produs</th>
						<th>Link produs</th>
						<th>Data adaugare</th>
						<th>Pret initial</th>
						<th>Pret curent</th>
						<th>Data ultima actualizare</th>
					</tr>
				</thead>
				<tbody>
                {% for produs in produse %}
				    {% if forloop.counter0|divisibleby:2 %}
					<tr class="table-active">
                    {% else %}
					<tr>
                    {% endif %}
						<td>{{forloop.counter}}</td>
						<td>
                            <a>{{produs.nume}}</a>
						</td>						
						<td>
                            <a>{{elevi|index:forloop.counter0|nume}} {{elevi|index:forloop.counter0|prenume}}</a>
                            <a href="/elev/{{aranjament|index:1}}" class="btn btn-primary float_right">Vizualizare Elev</a>
                  </td>
                        <td><a href="/aranjament/iddisciplina={{aranjament|index:0}}&idelev={{aranjament|index:1}}/modify" class="btn btn-primary float_right">Modificare Aranjament</a></td>
					</tr>
                {% endfor %}
				</tbody>
			</table>
			<a class="btn btn-primary float_right" style="margin-bottom: 10px;" href="/aranjament/add">Adauga Aranjament</a>
			<a class="btn btn-primary float_right" style="margin-bottom: 10px;" href="/aranjament/del">Stergere Aranjamente</a>
		</div>
	</div>
</div>
{% endblock %}